- name: install pip
  apt:
    name: python-pip
    state: present

- name: pip list
  raw: pip list

- name: pip install docker-py
  pip:
    name:
      - docker
      - docker-compose
#      - docker-compose==1.9.0

- name: pip list
  raw: pip list

- name: run container
  docker_container:
    name: wartest
    image: "{{ image }}"
    ports: 50080:8080
    state: started
 
